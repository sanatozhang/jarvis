# Python
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.env.bak.*
frontend/tsconfig.tsbuildinfo
.pids/
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
venv/
.venv/

# Node
node_modules/
.next/
out/

# Environment
.env
.env.local
.env.*.local

# Per-server runtime config overrides (settings-page writes go here, not config.yaml —
# see backend/app/config.py::write_local_override). Deliberately not versioned: each
# server's toggles are independent and must survive `git pull` during deploys.
config.local.yaml

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Data
*.db
*.sqlite3
data/
logs/
workspaces/

# Secrets
*.pem
*.key
feishu_auth.json
